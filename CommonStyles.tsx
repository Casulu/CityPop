import { StyleSheet } from "react-native"

export const common = StyleSheet.create({
    titleText: {
        marginTop: '15%',
        fontSize: 32,
        textAlign: 'center'
    },
    buttonText: {
        fontSize: 15,
        textAlign: 'center'
    }
})