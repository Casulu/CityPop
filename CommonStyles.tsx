import { StyleSheet } from "react-native"

export const common = StyleSheet.create({
    titleText: {
        marginTop: '15%',
        fontSize: 32,
        textAlign: 'center'
    },
    button: {
        backgroundColor: '#fad', 
        borderRadius: 4,
        margin: 10, 
        alignContent: 'center', 
        justifyContent: 'center'
    },
    buttonText: {
        fontSize: 15,
        textAlign: 'center'
    }
})